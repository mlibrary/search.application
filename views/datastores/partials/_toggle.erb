<%
  classes = ["button__ghost", "hide__no-javascript"] + Array(classes)

  postfix ||= ""
  body = ["Show fewer", postfix.presence].compact.join(" ")
%>
<button class="<%= classes.join(" ")%>" aria-expanded="true" aria-controls="<%= id %>" data-toggle="<%= count %>">
  <%= body %>
</button>
