<%= h1(
  body:
    titles.map do |title|
      <<-HTML
      <span class="record__#{title.css_class}" data-truncate="300">
        #{title.text}
      </span>
      HTML
    end.join,
  classes: ["record__title"]
) %>
